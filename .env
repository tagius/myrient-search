# ── Myrient Search Engine Configuration ──
# All values below are defaults. Edit any line to customize.

# Crawler settings
MYRIENT_BASE_URL=https://myrient.erista.me/files/
CRAWL_CONCURRENCY=10
CRAWL_DELAY_MS=50
CRAWL_TIMEOUT=30

# Sync schedule (cron format: minute hour day month weekday)
# Default: 3 AM on the 1st of every month
# Examples:
#   0 3 1 * *     = 3 AM, 1st of every month
#   0 3 * * 0     = 3 AM every Sunday
#   0 */6 * * *   = every 6 hours
SYNC_SCHEDULE=0 3 1 * *

# Server
HOST=0.0.0.0
PORT=8080

# Data directory (inside container)
DATA_DIR=/data

# Search
RESULTS_PER_PAGE=50
MAX_RESULTS=10000
